"use client";import { useState } from "react";type Status="new"|"contacted"|"booked"|"closed";type Lead={id:string;name:string;mobile:string;whatsapp?:string;telegram?:string;service:string;options:string;createdAt:string;status:Status};
export function LeadManager(){const [leads,setLeads]=useState<Lead[]>([]);return <section className="admin-panel"><div className="table-wrap"><table><thead><tr><th>نام</th><th>موبایل / پیام‌رسان</th><th>خدمت و انتخاب‌ها</th><th>تاریخ</th><th>وضعیت</th></tr></thead><tbody>{leads.map(l=><tr key={l.id}><td>{l.name}</td><td dir="ltr">{l.mobile}<small>{l.whatsapp||l.telegram}</small></td><td>{l.service}<small>{l.options}</small></td><td>{l.createdAt}</td><td><select value={l.status} onChange={e=>setLeads(x=>x.map(v=>v.id===l.id?{...v,status:e.target.value as Status}:v))}><option value="new">جدید</option><option value="contacted">تماس گرفته شد</option><option value="booked">رزرو شد</option><option value="closed">بسته شد</option></select></td></tr>)}</tbody></table></div>{leads.length===0&&<div className="empty-state">هنوز لیدی ثبت نشده است. داده واقعی پس از اتصال Supabase نمایش داده می‌شود.</div>}</section>}
